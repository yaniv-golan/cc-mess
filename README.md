# cc-mess

Inter-Claude-Code communication plugin. Enables multiple Claude Code instances to form a mesh network — discovering each other, communicating, delegating tasks, and developing emergent trust relationships.

Uses file-based transport under `~/.claude/channels/mess/` and integrates with Telegram for human visibility and control.

## Status

Design phase. Not yet implemented.
